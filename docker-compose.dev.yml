services:
  mcp-server:
    command: npm run dev
    volumes:
      - ./src:/usr/src/app/src
      - ./tsconfig.json:/usr/src/app/tsconfig.json
      - /usr/src/app/node_modules
    environment:
      - NODE_ENV=development
